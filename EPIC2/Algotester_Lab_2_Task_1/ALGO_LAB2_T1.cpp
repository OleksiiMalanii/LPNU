#include <iostream>
using namespace std;
int main () {
    int h[4], d[4];
    for (int i = 0; i < 4; ++i) {
        cin >> h[i];
    }
    for (int i = 0; i < 4; ++i) {
        cin >> d[i];
    }
    int h_maxm h_min, h_max_i, h_min_i;
}